import { Link, useLoaderData } from 'react-router-dom'
import { ROUTES } from '../../data/routes.js'
import { formatShortDate } from '../../utils/formatters.js'

export default function OrganizerDashboardPage() {
  const events = useLoaderData()
  return <section className="organizer-dashboard page-section" aria-labelledby="organizer-dashboard-title"><div className="container"><div className="organizer-welcome"><div><p className="eyebrow">Publique experiências mais acessíveis</p><h1 id="organizer-dashboard-title" tabIndex="-1">Bem-vindo à área do organizador</h1><p>Centralize seus eventos, comunique os recursos disponíveis e ajude cada pessoa a planejar sua participação.</p></div><div className="organizer-welcome__mark" aria-hidden="true">✦</div></div><div className="organizer-actions"><article className="organizer-action-card organizer-action-card--primary"><p className="organizer-action-card__icon" aria-hidden="true">＋</p><h2>Criar novo evento</h2><p>Registre detalhes, foto, públicos acolhidos e o suporte oferecido no local.</p><Link className="button button--primary" to={ROUTES.ORGANIZER_NEW_EVENT}>Cadastrar evento</Link></article><article className="organizer-action-card"><p className="organizer-action-card__icon" aria-hidden="true">☷</p><h2>Meus eventos</h2><p>Consulte a agenda de experiências publicadas no portal.</p><ul className="organizer-event-list">{events.map((event) => <li key={event.id}><strong>{event.title}</strong><span>{formatShortDate(event.date)} · {event.location}</span></li>)}</ul></article></div><div className="organizer-guide-cta"><div><p className="eyebrow">Acolhimento na prática</p><h2>Prepare sua equipe para receber todas as pessoas</h2></div><Link className="button button--secondary" to={ROUTES.ORGANIZER_ACCESSIBILITY}>Ler guia de acessibilidade</Link></div></div></section>
}
